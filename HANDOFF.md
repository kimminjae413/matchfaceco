# matchfac eco 랜딩 — 인수인계

> **마지막 작업: 2026-07-13.** 이 문서를 먼저 읽고 시작할 것.

## 지금 상태

| | |
|---|---|
| 라이브 | `matchfaceco.com` — main = 라이브 |
| 시안2 | **삭제됨** (2026-07-13, 사장님 지시). `public/test2/` 통째 제거 — 되살리지 말 것 |
| 작업 브랜치 | 없음 |

```bash
npm run build && npx vite preview   # 로컬 확인
git push origin main                # → Actions 자동배포(~1분)
```

라이브 = ①히어로 ②**영상 밴드(CinematicBand)** ③문제 ④핵심기능 ⑤방법론 ⑥현지네트워크(회로잎 배경) ⑦플랫폼 ⑧차별점 ⑨타깃·일정 ⑩문의 ⑪푸터.
한국어 `/` · 영어 `/?lang=en` · 예시 산정서 `/sample-report/` (영문 `/sample-report/en/`) · 법적 고지 `/legal/{privacy,terms}/`.

## 🔴 다음 세션 착수점

1. **사업계획서의 배출계수 0.56 정정 여부** — 사장님 결정 대기 (아래 '사업계획서에 0.56이 그대로 있다' 참조). 정부 제출본이라 임의로 안 고쳤음.
2. 그 외 '남은 작업' 목록 순서대로.

## ⚠️ 나(어시스턴트)에 대한 지시 — 사장님 지적 (2026-07-13, 같은 날 두 번)

**1) 화면에서 시각 요소가 사라지는 변경은 커밋 메시지 한 줄에 묻지 말고 따로 물어볼 것.**
지난 브랜치에서 Veo 영상·사진을 내가 임의로 걷어냈고, 머지 승인을 받을 때 "감성영상 섹션 삭제"라고만 적어 넘어갔다. 사장님이 "왜 니 맘대로 치웠냐"고 하신 게 정당하다.

**2) "복구"랍시고 배경에 깔아 묻지 말 것.** 위 지적 후 내가 영상을 히어로 *배경*으로 넣었는데, 오버레이 두 겹(`bg-ink/80` + 그라디언트)에 덮여 사실상 안 보였다. 그건 복구가 아니라 재삭제다.
→ **영상은 삭제 전과 같이 자기 섹션(`CinematicBand`)으로 히어로 다음에 있다. 이 상태가 정답이다. 건드리지 말 것.**

**3) "감성 자연영상 = 그린워싱"이라는 내 논리는 틀렸다. 다시 꺼내지 말 것.**
그린워싱은 *실체 없는 주장*을 자연 이미지로 덮을 때 문제가 된다. 이 페이지는 산정식·정부 공표 배출계수(0.6592)·예시 산정서를 전부 공개해 놓았다. 그 위에서 "탄소를 줄이는 일은 결국 자연을 지키는 일"이라고 말하는 건 브랜드가 왜 존재하는지를 말하는 것이다. 잎사귀 로고를 쓰는 회사가 그걸 말 못 할 이유가 없다. 사장님 말씀이 맞다 — **카본을 줄이면 환경이 산다. 그게 이 회사의 미션이다.**

**4) 룰을 기계적으로 적용해 멀쩡한 자산을 들어내지 말 것.** 위 셋은 전부 같은 실패의 변주다. "AI 랜딩 클리셰 제거" 같은 원칙은 판단을 돕는 도구지, 사장님이 만든 자산을 지우는 근거가 아니다.

---

## ✅ 종결: 배출계수 출처 (2026-07-13 리서치로 확정)

**`0.56 kgCO₂e/kWh` 는 베트남 정부 공표 문서 어디에도 없는 값이었다. 다시 쓰지 말 것.**

- 베트남 공식 전력망 계수는 천연자원환경부 **기후변화국(Cục Biến đổi khí hậu, DCC)** 이 매년 **공문(Công văn)** 으로 공표한다.
  - 2023년(최신 확정) **0.6592 tCO₂/MWh** — 공문 **1726/BĐKH-PTCBT** (2024 발표)
  - 2022년 0.6766 — 공문 327/BĐKH-PTCBT
  - 2021년 0.7221 / 2020년 0.8041 / 2019년 0.8458
- **MONRE 결정 4432/QĐ-BTNMT(2022)** 는 배출계수 *목록*을 공표하는 결정이지 그리드 계수 숫자를 담은 문서가 아니다. **통람 38/2023/TT-BCT** 는 산업부문 MRV *절차* 규정이라 숫자가 없다. 둘 중 어느 것도 "0.56의 출처"로 인용하면 틀린다.
- 0.56은 국제금융기구(IFI) 프로젝트 파이낸싱 심사용 default grid factor 데이터셋 값으로 추정된다(2차 출처 기반, 1차 미대조). 정부 공식치보다 **15~20% 낮아** 배출량을 실제보다 작게 보이게 만든다 — 그린워싱 지적의 전형.
- → Formula 섹션에 **DCC 공문 1726 / 0.6592** 를 출처와 함께 명시함.

---

## 이 브랜치에서 한 일

### P0 — 신뢰를 깎던 사실관계
- **제조사 수 불일치 통일**: 히어로만 32곳, 나머지는 30곳+ 였음 → 문서 기준 **30곳+** 로 단일화
- **없는 실적 제거**: 대시보드 누적 카운트 → 기능 상태 표기 + "예시 화면" 명시 (☝️ 결정 대기 2번)
- ~~근거 없는 "36건 제조 아웃소싱 운영 경험" 삭제~~ → **❌ 이 판단은 틀렸다. 2026-07-13 정정·복원함.** 사업계획서 '보유 역량'·'개발 동기'에 명시되어 있다("36건의 제조 아웃소싱 프로젝트 운영", "36건의 제조 프로젝트 총괄 실무"). 지금은 '만드는 사람' 섹션의 뼈대다. **자료를 안 열어보고 '근거 없음'이라 단정하지 말 것 — `_biz_plan.txt`가 폴더에 있다.**
- **배출계수 출처 정직화** (☝️ 결정 대기 3번)
- **푸터 주소를 사업자등록증 표기로 정정** (건물명 포함)

### P1 — AI 랜딩 클리셰 제거
- 히어로 **터미널(신호등 점 3개 + 타이핑) → PCF 산정서 문서**로 교체. 이 제품 구매자는 구매·소싱·ESG 담당자지 개발자가 아니다 — 터미널은 애초에 장르 오류였음
- **CinematicBand(나뭇잎·새 영상) 통째 삭제** — CBAM 규제 대응 상품에 감성 자연 영상은 그린워싱 신호
- 알약 배지 3종 / 방사형 글로우 / 격자 배경 / 스태거 fadeUp 제거
- 영어 대문자 모노 eyebrow(THE PROBLEM 등) → 한국어 라벨 + 괘선
- 헤드라인 폰트 **Wanted Sans** 도입, Quicksand 제거
- **다크 면을 근접 블랙(#05100C) → BI 딥 포레스트(#083630)로.** "근접 블랙 + 단일 애시드 그린"이 현재 가장 흔한 AI 랜딩 룩이라, 그린(BI라 못 버림)은 두고 바닥을 올려 축을 이동
- VisualBand의 **AI 생성 공장 작업자 목업 사진 제거** (실사 확보 시 재투입)

### 레이아웃 결함
- Features 벤토 그리드 깨짐 (6칸에 4+2/2+2 → 마지막 2칸 공백) → 번호 목록으로 재구성
- 비교표 강조 열이 고정 내비 아래로 파고들어 헤더 잘리던 문제

### 전환
- `mailto:` 링크뿐이던 CTA에 **구조화된 문의 폼**(회사·담당자·공정·수량) + **카카오 채널** 추가
- 서버 없는 정적 호스팅이라 입력값을 메일 본문으로 조립하는 방식. 나중에 Formspree/Tally 엔드포인트로 교체만 하면 됨

---

## ✅ 예시 산정서 = 완료 (라이브)

`matchfaceco.com/sample-report/` — `public/sample-report/index.html` 정적 단일 파일. 히어로 산정서 아래와 방법론 섹션에서 링크. 브라우저 인쇄로 PDF 저장되므로 **별도 PDF 파일을 만들 필요 없음**.
수치는 내부 정합이 맞춰져 있다 — 전력 0.168 + 소재 0.215 + 물류 0.037 = **0.420 kgCO₂e/EA**, 업계평균 0.516 대비 **−18.6%**, 120,000 EA = 50.4 tCO₂e. **하나를 고치면 나머지도 같이 고칠 것.**
문서·수치는 전부 가상 예시이며 상단에 '서식 예시 · 실제 발행본 아님'을 고지해 뒀다.

## ✅ 법적 고지 = 완료 (라이브)

`/legal/privacy/` (개인정보처리방침) · `/legal/terms/` (이용약관). `public/legal/` 정적 파일, 공통 스타일 `legal.css`. 푸터 + 문의 폼 하단에서 링크.

**두 문서 모두 "실제 동작"만 적었다 — 지키지 못할 문장을 쓰면 그게 곧 위반이 된다.**
- 사이트에 회원가입·쿠키·분석 스크립트·localStorage가 **하나도 없다**(코드로 확인함). 방침에 그대로 명시. **나중에 GA·픽셀·Formspree 같은 걸 붙이면 방침을 반드시 같이 고칠 것.**
- 폼은 mailto라 "실제로 메일을 보낸 경우에만" 정보가 전달됨을 밝힘.
- 처리위탁 3곳(Google·GitHub·카카오) + 국외이전 고지.
- 약관 **제6조**에 산정 결과의 한계를 못 박음: 제3자 검증 아님 / 활동자료 정확성에 의존 / 배출계수 갱신으로 과거 결과가 바뀔 수 있음 / LCA를 대체하지 않음.
- 관할은 "민사소송법에 따른 관할 법원"으로 뒀다(전속관할을 회사 소재지로 지정하면 약관규제법상 불리 조항 시비가 붙을 수 있음).

## 🔴 사업계획서에 0.56이 그대로 있다 (사장님 확인 필요)

`_biz_plan.txt` 232행: **"국가별 전력 배출 계수(예: 베트남 1kWh = 0.56kg CO2e)"**
→ **랜딩의 0.56은 여기서 온 것이었다.** 그런데 이 값은 베트남 정부 공표 문서 어디에도 없고, 공식치(2023년 0.6592)보다 15~20% 낮다(위 '종결 2건' 참조).
랜딩은 고쳤지만 **사업계획서·IR 자료는 아직 0.56이다.** 이 문서를 복사해 만드는 제안서·심사자료마다 틀린 계수가 퍼진다. 정부 과제 심사에서 지적당하기 딱 좋은 자리 → 사장님께 고칠지 확인할 것.

## ❌ '만드는 사람' 섹션 = 삭제됨 (2026-07-13, 사장님 지시)

`src/components/Team.tsx` 삭제. 내비·푸터 링크, `content.ts` 의 `team` 딕셔너리(ko/en), `export const team` 전부 제거.
**되살리지 말 것 — 사장님이 "저렇게 짧게 쓸 거면 지워"라고 하셨고, 판단이 맞다.** 사업계획서를 다시 뒤져도 대표 이력·학력·팀원·특허가 **없다**(특허출원은 목표지 실적이 아님). 넣을 사실이 없어서 명함 4줄(대표/법인/거점/단계)에 그쳤던 것이고, 그 상태로는 B2B 신뢰에 기여하는 바가 없었다.
- **36건은 살렸다** — `formula.origin` 으로 옮겨 '방법론' 섹션 본문 아래 괘선 인용으로 남겼다. 산정 로직의 출처를 말하는 자리라 오히려 여기가 맞다.
- 사이니지 사진(`public/brand/glass-sign.webp`)은 이 섹션과 함께 화면에서 빠졌다(파일은 남아 있음). **사장님 승인 하에 지운 것** — 지난번처럼 임의 제거가 아니다.
- 되살리려면 **대표 성함·직함·사진, 제조/무역 경력, 팀 구성**을 사장님께 받아야 한다. 그게 없으면 다시 지금 꼴이 된다.

## ✅ 영문판 = 완료 (라이브)

`matchfaceco.com/?lang=en` · 영문 산정서 `matchfaceco.com/sample-report/en/`

- **카피 단일 소스 = `src/content.ts` 의 ko / en 두 딕셔너리.** 컴포넌트에 하드코딩된 한국어는 전부 걷어냈다. **카피를 고칠 땐 두 언어를 같이 고칠 것.**
- 언어 결정 = `src/lang.ts`, URL 쿼리 `?lang=en`. 정적 호스팅이라 서버 협상이 없다. 브라우저 언어 자동 감지는 **일부러 안 했다** — 국내 담당자가 영문을 보게 되는 사고가 더 나쁘다.
- **수치는 언어가 달라도 똑같아야 한다** (30곳+, 18.6%, ISO 14067, 0.6592, 36건). 한쪽만 고치면 둘 다 거짓이 된다.
- 예시 산정서는 정적 HTML이라 쿼리로 안 바뀐다 → 영문은 별도 경로(`/sample-report/en/`), `sampleReportHref` 로 분기.
- **법적 고지(개인정보처리방침·이용약관)는 한국어만.** 국내 법령 문서라 원문이 기준. 영문 폼 안내에 (Korean) 표기.

## 남은 작업 (우선순위)

1. 사업계획서·서비스소개 문서의 **0.56 계수 정정** (☝️ 위 확인 필요 항목)
2. 실제 계약 제조사가 생기면 예시 산정서를 **실물 익명화 산정서**로 교체 (지금은 가상)
3. 문의 폼을 Formspree/Tally 엔드포인트로 교체 (mailto는 전환율이 샌다) — **교체 시 개인정보처리방침의 위탁·수집 항목도 같이 갱신**
4. 대표 사진·팀원이 생기면 '만드는 사람' 섹션에 추가
5. 베트남어판 — 제조사가 직접 읽을 언어. `content.ts` 에 `vi` 딕셔너리를 더하면 구조는 그대로 확장된다

---

## 함정 (겪은 것들)

- **Tailwind**: 커스텀 색 opacity는 표준 스텝만. `@apply bg-ink/12` 는 빌드 실패 → `/10`, `/20`
- **정적 페이지의 이미지**: `src/assets/`는 번들러 전용이라 `public/` 밖 정적 HTML에서 못 쓴다. 로고는 `public/brand/wordmark-dark.png` 에도 사본이 있어야 한다 (`/sample-report/`가 이걸 참조)
- **vite preview는 없는 경로에 index.html을 200으로 돌려준다** — 이미지가 깨졌는데 curl은 200이면 이 함정이다. 눈으로 볼 것
- **레퍼런스 판단**: 사장님이 준 21st.dev 템플릿을 안 보고 "다크 네온"이라 단정했다가 틀림. 실제론 순백·무강조색이었다. **iframe src 따서 라이브 데모를 열 것**
- **BI는 그대로 쓸 것** — 로고·색 임의 재현 금지 (사장님 지시)
- **커밋 메시지**: bkit 훅이 `$(cmd <<EOF)` 를 막는다 → 메시지를 파일로 쓰고 `git commit -F`
